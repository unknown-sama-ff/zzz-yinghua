# ============================================================
#  影画工坊 / Yinghua Workshop — server environment
#  Copy to `.env` and fill in real values. NEVER commit `.env`.
#  All keys are read server-side only; the frontend never sees them.
# ============================================================

# Port for the Node proxy (default 8787)
PORT=8787

# Allowed browser origin for CORS (the Vite dev server in development)
CORS_ORIGIN=http://localhost:5173

# --- Provider: seedance (image-to-image / image edit) ---
SEEDANCE_API_KEY=
SEEDANCE_BASE_URL=https://api.seedance.example/v1

# --- Provider: gpt-image (OpenAI gpt-image-1) ---
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# Outbound request timeout in ms (default 120000 — image gen is slow)
UPSTREAM_TIMEOUT_MS=120000
